def print_numbers_times_2(numbers_list):
    for number in numbers_list:
        print(number * 2)

-- La complejidad del algoritmo es O(n) ya que realiza una operacion por cada elemento de la lista. 


def check_if_lists_have_an_equal(list_a, list_b):
    for element_a in list_a:
        for element_b in list_b:
            if element_a == element_b:
                return True

    return False

-- En este caso, hay dos ciclos aninados por lo que la complejidad es O(n2)


def print_10_or_less_elements(list_to_print):
    list_len = len(list_to_print)
    for index in range(min(list_len, 10)):
        print(list_to_print[index])

-- Este caso está limitado a 10 datos en la lista por lo que su complejidad es O(1), es constante. 


def generate_list_trios(list_a, list_b, list_c):
	result_list = []
	for element_a in list_a:
		for element_b in list_b:
			for element_c in list_c:
				result_list.append(f'{element_a} {element_b} {element_c}')
				
	return result_list 

-- Acá se analizarían 3 listas por lo que es O(n³)